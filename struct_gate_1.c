#include <stdio.h>
struct node
{
    int i;
    float j;
};
int main()
{
    struct node *s[10];//predence of [] is higher so s is an array and pointer to a structer type node;
    return 0;
}