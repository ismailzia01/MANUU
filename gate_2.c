#include<stdio.h>
int fun(int num);
int main()
{
    int count = 0;
    while(num)
    {
        count++;
        num >> 1;
    }
    return count;
    //the value returned by fun(435) is ....?
    //GATE 2014

}